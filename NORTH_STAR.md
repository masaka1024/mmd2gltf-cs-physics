# NORTH STAR — 何を「正しい」とするか

このリポジトリは MMD (PMX) の物理を Bullet 2.75 互換で再実装したものです。
変更の採否がぶれないよう、**判定者を1つに固定**します。ここが他のドキュメントより上位です。

---

## 1. 判定者は「参照アンカー付きの |Δp|」だけ

採否を決めてよい指標は、**参照との比**が取れるものに限ります。

| 判定者 | 参照アンカー | 何を測るか |
|---|---|---|
| `BoneDp` (静止) | PMXエディタの Fix ベイク VMD (静止ダミー) | 駆動なしで揺れ物がどれだけ動くか |
| `drivedp` (駆動) | 焼き込み済み参照 CSV | 動いている最中の揺れ量 (部位別) |
| `bulletref` | 純 Bullet 2.75 を同一の網で回した結果 | 実装差そのもの |
| `bonecheck` / `hairfid` | 焼き込み済み参照 CSV | 駆動忠実度 (傾き・12窓比・貫入) |

**|Δp| の定義は全ハーネスで同一**です。ボーン姿勢 = `bodyWorld * BodyOffsetFromBone⁻¹`、
30fps 標本、後半 1/3 でボーン別の中央値/p90 を取り、そのボーン間中央値を代表値とします。

比の読み方は **1.00 = 参照と同じ量**。**0 が正解ではありません。**

---

## 2. 参照の無いスイープは「スクリーニング専用」

31モデルの `restosc` スイープ (残留角速度 |w|・拘束違反) には**参照アンカーがありません**。
これらで採否を決めてはいけません。用途は次の3つに限ります。

- **NaN / 発散**の検出
- **粗い退行**の検出 (桁で悪化していないか)
- 未知モデルでの**事故の早期発見**

### なぜ格下げしたか

**理由1: タスク6 の教訓がスイープへ適用されていなかった。**
「揺れ物は止まるのが正解」ではありません。参照 (PMXエディタ) 自身が揺れています。
参照値を知らずに |w| の大小を比べるのは、**暗黙にゼロと比較している**のと同じです。
この誤りは静止 |Δp| の側では 2026-08 に是正済みでしたが、スイープ側には適用が漏れていました。

**理由2: 実際に符号が矛盾した。** 同じ変更・同じモデル・同じ剛体集合で、
|w| と |Δp| が逆を向いた実例が出ています。

| 対象 | 残留角速度 \|w\| | 参照アンカー付き \|Δp\| |
|---|---|---|
| モデルA の髪 (受理閾値 ON) | 67.9 → 61.4 (**改善**) | 0.0290 → 0.0390 (**悪化**) |
| モデルA_ettc髪 (同上) | 54.2 → 68.6 (**悪化**) | 0.0257 → 0.0208 (**改善**) |

|w| は「速度が小さいほど良い」という**ゼロ基準**の指標なので、
参照より静かになっても「改善」と出ます。**動かなさすぎも不正解**である以上、判定者にはできません。

---

## 3. この階層の下で再審となるもの

判定根拠を失ったため、**却下ではなく再審**として扱います。

| 施策 | 棚上げの根拠だったもの | 現在の扱い |
|---|---|---|
| 3点構成 (`BulletAngleConvention` + `AngularMixedAxes` + `LinearLeverMode=1`) | 31モデルスイープ (\|w\|) で負け越し | **再審**。髪の静止 \|Δp\| では純 Bullet 比 25.3× → 0.53〜0.59× と支配項だった |
| `BulletRotationIntegration` (ROTEXP) | 31モデルスイープ (\|w\|) で FAIL | **再審**。参照アンカー付きの判定を取り直す |

---

## 4. 変更を入れるときの順序

1. **実装差なら Bullet 2.75 の実ソースを引く。** 当てるパラメータを作らない。
2. **行レベルで一致を確認する** (`bulletref` との突き合わせ)。
3. **参照アンカー付きの判定者を通す** (上表)。
4. **スクリーニングを通す** (NaN・発散・粗退行)。
5. 通ってから既定変更・4箇所同期。

順序を飛ばさないこと。特に 2 を飛ばして 3 の数字だけで判断すると、
「たまたま良く見える誤り」を採用してしまいます。

---

## 5. 合わせ先

**PMXエディタ**です。MMD 本体には寄せません (本体に物理ベイク機能が無く参照が取れないため)。
参照側の条件は実測済み: dt=1/60 秒・実効 60Hz・重力 98.1。

より詳しい方針・逸脱一覧・実装の根拠は [docs/DESIGN.md](docs/DESIGN.md) を参照してください。
